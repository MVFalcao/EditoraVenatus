@model IEnumerable<EditoraAPI.Models.TB_Estoque>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TB_Livro.Titulo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TB_Livro.Titulo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Estoque }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Estoque }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Estoque })
        </td>
    </tr>
}

</table>
