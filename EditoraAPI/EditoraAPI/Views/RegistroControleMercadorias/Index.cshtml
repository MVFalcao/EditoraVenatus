@model IEnumerable<EditoraAPI.Models.RegistroControleMercadoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Livro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataReg)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Livro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantidade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataReg)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_RegConMerc }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_RegConMerc }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_RegConMerc })
        </td>
    </tr>
}

</table>
