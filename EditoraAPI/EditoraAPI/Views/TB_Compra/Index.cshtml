@model IEnumerable<EditoraAPI.Models.TB_Compra>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Preco_total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data_Pag)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TB_Cliente.ID_Cliente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TB_Tipo.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Preco_total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data_Pag)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TB_Cliente.ID_Cliente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TB_Tipo.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Compra }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Compra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Compra })
        </td>
    </tr>
}

</table>
