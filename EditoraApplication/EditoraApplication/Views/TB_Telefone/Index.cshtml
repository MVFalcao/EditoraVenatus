@model IEnumerable<EditoraApplication.Models.TB_Telefone>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Telefone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TB_Autor.CPF)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TB_Cliente.ID_Cliente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Telefone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TB_Autor.CPF)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TB_Cliente.ID_Cliente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Telefone }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Telefone }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Telefone })
        </td>
    </tr>
}

</table>
