@model IEnumerable<EditoraApplication.Models.TB_Cupom_Livro>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TB_Cupom.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TB_Livro.Titulo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TB_Cupom.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TB_Livro.Titulo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Cupom_Livro }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Cupom_Livro }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Cupom_Livro })
        </td>
    </tr>
}

</table>
